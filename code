#include<reg51.h>
void Delay(unsigned int itime)
{	
	unsigned int i,j;
	for(i=0;i<itime;i++);
	{
		for(j=0;j<1275;j++);
	}
}
void main(void)
{
	unsigned int z;
	for(z=0;z<=255;z++)
	{
		P1=z;
		Delay(1000);
	}
}
